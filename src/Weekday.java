public class Weekday {
    // TODO: Implement a method to check if a given day is a weekday (Monday to Friday)
    public boolean isWeekday(int day) {
        // Returnerar true om dayName är någon av dagarna måndag till fredag
        // False annars

        return day >= 1 && day <= 5;
    }
}
